To make a SSL connection and to view the SSL certificate:

1.clone the repo 

2. The URL to establish the connection with is hard coded into dest_url in the main function.  To change the URL, change dest_url.

3. run the following to compile :

rm sslconnect

gcc -o sslconnect sslconnect.c -lssl -lyrcypto



4. Then, to run the program: ./sslconnect
